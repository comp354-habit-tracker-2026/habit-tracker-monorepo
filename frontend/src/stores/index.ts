/**
 * Global state stores.
 *
 * Add Zustand or similar stores here for application-wide UI state
 * (e.g. notification queue, sidebar open state).
 *
 * Server/async data should live in React Query caches, NOT here.
 * Component-local UI state should live in component useState, NOT here.
 *
 * @see https://github.com/alan2207/bulletproof-react/blob/master/docs/state-management.md
 */

// Example: export { useNotificationStore } from './notification-store';
